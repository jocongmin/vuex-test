import { REQUEST_CONTENT_LIST } from '../mutation-types'

const state = {
    tagList:25
}

const mutations = {
    [REQUEST_CONTENT_LIST] (state) {
        state.tagList = 4654654
    }
}

export default {
    state,
    mutations
}